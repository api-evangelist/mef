generated: '2026-07-25'
method: derived
source: mcp/mef-mcp.yml + openapi/*.yml (kylie release)
surfaces:
  openapi:
    path: openapi/
    documents: 94
    operations: 688
    gated: false
  asyncapi:
    path: asyncapi/
    documents: 1
    gated: false
  mcp:
    servers: 47
    hosted: false
    gated: tools/list requires running the published container against a Seller endpoint; no anonymous
      introspection is possible
  graphql: null
binding_basis: Mplify generates its MCP servers directly from its own OpenAPI documents with FastMCP.from_openapi();
  the tool set is therefore a faithful projection of the operation set, one tool per operation.
coverage:
  mcp_servers: 47
  apis_with_mcp: 47
  tools_bound: 449
  mcp_only: 0
  rest_operations_total: 688
  rest_operations_with_tool: 449
crosswalk:
- tool: listAppointment
  category: appointment_management
  irp: LSO Sonata
  rest:
  - listAppointment
  binding: rest
  openapi: openapi/mef-lso-sonata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createAppointment
  category: appointment_management
  irp: LSO Sonata
  rest:
  - createAppointment
  binding: rest
  openapi: openapi/mef-lso-sonata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveAppointment
  category: appointment_management
  irp: LSO Sonata
  rest:
  - retrieveAppointment
  binding: rest
  openapi: openapi/mef-lso-sonata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: patchAppointment
  category: appointment_management
  irp: LSO Sonata
  rest:
  - patchAppointment
  binding: rest
  openapi: openapi/mef-lso-sonata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: cancelAppointment
  category: appointment_management
  irp: LSO Sonata
  rest:
  - cancelAppointment
  binding: rest
  openapi: openapi/mef-lso-sonata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createSearchTimeSlot
  category: appointment_management
  irp: LSO Sonata
  rest:
  - createSearchTimeSlot
  binding: rest
  openapi: openapi/mef-lso-sonata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: appointment_management
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: appointment_management
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: appointment_management
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCustomerBill
  category: billing_management
  irp: LSO Sonata
  rest:
  - listCustomerBill
  binding: rest
  openapi: openapi/mef-lso-sonata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCustomerBill
  category: billing_management
  irp: LSO Sonata
  rest:
  - retrieveCustomerBill
  binding: rest
  openapi: openapi/mef-lso-sonata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCustomerBillItem
  category: billing_management
  irp: LSO Sonata
  rest:
  - retrieveCustomerBillItem
  binding: rest
  openapi: openapi/mef-lso-sonata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: billing_management
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: billing_management
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: billing_management
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCimIncident
  category: circuit_impairment_and_maintenance
  irp: LSO Sonata
  rest:
  - listCimIncident
  binding: rest
  openapi: openapi/mef-lso-sonata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCimIncident
  category: circuit_impairment_and_maintenance
  irp: LSO Sonata
  rest:
  - retrieveCimIncident
  binding: rest
  openapi: openapi/mef-lso-sonata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: circuit_impairment_and_maintenance
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: circuit_impairment_and_maintenance
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: circuit_impairment_and_maintenance
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createGeographicAddressValidation
  category: geographic_address_management
  irp: LSO Sonata
  rest:
  - createGeographicAddressValidation
  binding: rest
  openapi: openapi/mef-lso-sonata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveGeographicAddressValidation
  category: geographic_address_management
  irp: LSO Sonata
  rest:
  - retrieveGeographicAddressValidation
  binding: rest
  openapi: openapi/mef-lso-sonata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveGeographicAddress
  category: geographic_address_management
  irp: LSO Sonata
  rest:
  - retrieveGeographicAddress
  binding: rest
  openapi: openapi/mef-lso-sonata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: geographic_address_management
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: geographic_address_management
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: geographic_address_management
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listGeographicSite
  category: geographic_site_management
  irp: LSO Sonata
  rest:
  - listGeographicSite
  binding: rest
  openapi: openapi/mef-lso-sonata-geographic-site-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveGeographicSite
  category: geographic_site_management
  irp: LSO Sonata
  rest:
  - retrieveGeographicSite
  binding: rest
  openapi: openapi/mef-lso-sonata-geographic-site-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCategory
  category: product_catalog
  irp: LSO Sonata
  rest:
  - listCategory
  binding: rest
  openapi: openapi/mef-lso-sonata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCategory
  category: product_catalog
  irp: LSO Sonata
  rest:
  - retrieveCategory
  binding: rest
  openapi: openapi/mef-lso-sonata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProductOffering
  category: product_catalog
  irp: LSO Sonata
  rest:
  - listProductOffering
  binding: rest
  openapi: openapi/mef-lso-sonata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProductOffering
  category: product_catalog
  irp: LSO Sonata
  rest:
  - retrieveProductOffering
  binding: rest
  openapi: openapi/mef-lso-sonata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProductSpecification
  category: product_catalog
  irp: LSO Sonata
  rest:
  - listProductSpecification
  binding: rest
  openapi: openapi/mef-lso-sonata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProductSpecification
  category: product_catalog
  irp: LSO Sonata
  rest:
  - retrieveProductSpecification
  binding: rest
  openapi: openapi/mef-lso-sonata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: product_catalog
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: product_catalog
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: product_catalog
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProduct
  category: product_inventory
  irp: LSO Sonata
  rest:
  - listProduct
  binding: rest
  openapi: openapi/mef-lso-sonata-product-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProduct
  category: product_inventory
  irp: LSO Sonata
  rest:
  - retrieveProduct
  binding: rest
  openapi: openapi/mef-lso-sonata-product-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: productOfferingAvailabilityRequest
  category: product_offering_availability_and_pricing_discovery
  irp: LSO Sonata
  rest:
  - productOfferingAvailabilityRequest
  binding: rest
  openapi: openapi/mef-lso-sonata-product-offering-availability-and-pricing-discovery-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: pricingDiscoveryRequest
  category: product_offering_availability_and_pricing_discovery
  irp: LSO Sonata
  rest:
  - pricingDiscoveryRequest
  binding: rest
  openapi: openapi/mef-lso-sonata-product-offering-availability-and-pricing-discovery-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: product_offering_qualification
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: product_offering_qualification
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: product_offering_qualification
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProductOfferingQualification
  category: product_offering_qualification
  irp: LSO Sonata
  rest:
  - listProductOfferingQualification
  binding: rest
  openapi: openapi/mef-lso-sonata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createProductOfferingQualification
  category: product_offering_qualification
  irp: LSO Sonata
  rest:
  - createProductOfferingQualification
  binding: rest
  openapi: openapi/mef-lso-sonata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProductOfferingQualification
  category: product_offering_qualification
  irp: LSO Sonata
  rest:
  - retrieveProductOfferingQualification
  binding: rest
  openapi: openapi/mef-lso-sonata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProductOrder
  category: product_order
  irp: LSO Sonata
  rest:
  - listProductOrder
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createProductOrder
  category: product_order
  irp: LSO Sonata
  rest:
  - createProductOrder
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProductOrder
  category: product_order
  irp: LSO Sonata
  rest:
  - retrieveProductOrder
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: patchProductOrder
  category: product_order
  irp: LSO Sonata
  rest:
  - patchProductOrder
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelProductOrder
  category: product_order
  irp: LSO Sonata
  rest:
  - listCancelProductOrder
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelProductOrder
  category: product_order
  irp: LSO Sonata
  rest:
  - createCancelProductOrder
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelProductOrder
  category: product_order
  irp: LSO Sonata
  rest:
  - retrieveCancelProductOrder
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCharge
  category: product_order
  irp: LSO Sonata
  rest:
  - listCharge
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCharge
  category: product_order
  irp: LSO Sonata
  rest:
  - retrieveCharge
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: patchCharge
  category: product_order
  irp: LSO Sonata
  rest:
  - patchCharge
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: product_order
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: product_order
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: product_order
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyProductOrderItemRequestedDeliveryDate
  category: product_order
  irp: LSO Sonata
  rest:
  - listModifyProductOrderItemRequestedDeliveryDate
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyProductOrderItemRequestedDeliveryDate
  category: product_order
  irp: LSO Sonata
  rest:
  - createModifyProductOrderItemRequestedDeliveryDate
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyProductOrderItemRequestedDeliveryDate
  category: product_order
  irp: LSO Sonata
  rest:
  - retrieveModifyProductOrderItemRequestedDeliveryDate
  binding: rest
  openapi: openapi/mef-lso-sonata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listQuote
  category: quote
  irp: LSO Sonata
  rest:
  - listQuote
  binding: rest
  openapi: openapi/mef-lso-sonata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createQuote
  category: quote
  irp: LSO Sonata
  rest:
  - createQuote
  binding: rest
  openapi: openapi/mef-lso-sonata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveQuote
  category: quote
  irp: LSO Sonata
  rest:
  - retrieveQuote
  binding: rest
  openapi: openapi/mef-lso-sonata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: cancelQuote
  category: quote
  irp: LSO Sonata
  rest:
  - cancelQuote
  binding: rest
  openapi: openapi/mef-lso-sonata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: declineQuote
  category: quote
  irp: LSO Sonata
  rest:
  - declineQuote
  binding: rest
  openapi: openapi/mef-lso-sonata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: quote
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: quote
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: quote
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTroubleTicket
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - listTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createTroubleTicket
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - createTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTroubleTicket
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - retrieveTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: patchTroubleTicket
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - patchTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: cancelTroubleTicket
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - cancelTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: closeTroubleTicket
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - closeTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: reopenTroubleTicket
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - reopenTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listIncident
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - listIncident
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveIncident
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - retrieveIncident
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: trouble_ticket_management
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveWorkOrder
  category: work_order_management
  irp: LSO Sonata
  rest:
  - retrieveWorkOrder
  binding: rest
  openapi: openapi/mef-lso-sonata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listWorkOrder
  category: work_order_management
  irp: LSO Sonata
  rest:
  - listWorkOrder
  binding: rest
  openapi: openapi/mef-lso-sonata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: work_order_management
  irp: LSO Sonata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-sonata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: work_order_management
  irp: LSO Sonata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-sonata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: work_order_management
  irp: LSO Sonata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-sonata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listAppointment
  category: appointment_management
  irp: LSO Cantata
  rest:
  - listAppointment
  binding: rest
  openapi: openapi/mef-lso-cantata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createAppointment
  category: appointment_management
  irp: LSO Cantata
  rest:
  - createAppointment
  binding: rest
  openapi: openapi/mef-lso-cantata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveAppointment
  category: appointment_management
  irp: LSO Cantata
  rest:
  - retrieveAppointment
  binding: rest
  openapi: openapi/mef-lso-cantata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: patchAppointment
  category: appointment_management
  irp: LSO Cantata
  rest:
  - patchAppointment
  binding: rest
  openapi: openapi/mef-lso-cantata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: cancelAppointment
  category: appointment_management
  irp: LSO Cantata
  rest:
  - cancelAppointment
  binding: rest
  openapi: openapi/mef-lso-cantata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createSearchTimeSlot
  category: appointment_management
  irp: LSO Cantata
  rest:
  - createSearchTimeSlot
  binding: rest
  openapi: openapi/mef-lso-cantata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: appointment_management
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: appointment_management
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: appointment_management
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-appointment-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCustomerBill
  category: billing_management
  irp: LSO Cantata
  rest:
  - listCustomerBill
  binding: rest
  openapi: openapi/mef-lso-cantata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCustomerBill
  category: billing_management
  irp: LSO Cantata
  rest:
  - retrieveCustomerBill
  binding: rest
  openapi: openapi/mef-lso-cantata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCustomerBillItem
  category: billing_management
  irp: LSO Cantata
  rest:
  - retrieveCustomerBillItem
  binding: rest
  openapi: openapi/mef-lso-cantata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: billing_management
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: billing_management
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: billing_management
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-billing-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCimIncident
  category: circuit_impairment_and_maintenance
  irp: LSO Cantata
  rest:
  - listCimIncident
  binding: rest
  openapi: openapi/mef-lso-cantata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCimIncident
  category: circuit_impairment_and_maintenance
  irp: LSO Cantata
  rest:
  - retrieveCimIncident
  binding: rest
  openapi: openapi/mef-lso-cantata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: circuit_impairment_and_maintenance
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: circuit_impairment_and_maintenance
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: circuit_impairment_and_maintenance
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-circuit-impairment-and-maintenance-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createGeographicAddressValidation
  category: geographic_address_management
  irp: LSO Cantata
  rest:
  - createGeographicAddressValidation
  binding: rest
  openapi: openapi/mef-lso-cantata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveGeographicAddressValidation
  category: geographic_address_management
  irp: LSO Cantata
  rest:
  - retrieveGeographicAddressValidation
  binding: rest
  openapi: openapi/mef-lso-cantata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveGeographicAddress
  category: geographic_address_management
  irp: LSO Cantata
  rest:
  - retrieveGeographicAddress
  binding: rest
  openapi: openapi/mef-lso-cantata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: geographic_address_management
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: geographic_address_management
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: geographic_address_management
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-geographic-address-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listGeographicSite
  category: geographic_site_management
  irp: LSO Cantata
  rest:
  - listGeographicSite
  binding: rest
  openapi: openapi/mef-lso-cantata-geographic-site-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveGeographicSite
  category: geographic_site_management
  irp: LSO Cantata
  rest:
  - retrieveGeographicSite
  binding: rest
  openapi: openapi/mef-lso-cantata-geographic-site-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCategory
  category: product_catalog
  irp: LSO Cantata
  rest:
  - listCategory
  binding: rest
  openapi: openapi/mef-lso-cantata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCategory
  category: product_catalog
  irp: LSO Cantata
  rest:
  - retrieveCategory
  binding: rest
  openapi: openapi/mef-lso-cantata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProductOffering
  category: product_catalog
  irp: LSO Cantata
  rest:
  - listProductOffering
  binding: rest
  openapi: openapi/mef-lso-cantata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProductOffering
  category: product_catalog
  irp: LSO Cantata
  rest:
  - retrieveProductOffering
  binding: rest
  openapi: openapi/mef-lso-cantata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProductSpecification
  category: product_catalog
  irp: LSO Cantata
  rest:
  - listProductSpecification
  binding: rest
  openapi: openapi/mef-lso-cantata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProductSpecification
  category: product_catalog
  irp: LSO Cantata
  rest:
  - retrieveProductSpecification
  binding: rest
  openapi: openapi/mef-lso-cantata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: product_catalog
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: product_catalog
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: product_catalog
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-product-catalog-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProduct
  category: product_inventory
  irp: LSO Cantata
  rest:
  - listProduct
  binding: rest
  openapi: openapi/mef-lso-cantata-product-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProduct
  category: product_inventory
  irp: LSO Cantata
  rest:
  - retrieveProduct
  binding: rest
  openapi: openapi/mef-lso-cantata-product-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: productOfferingAvailabilityRequest
  category: product_offering_availability_and_pricing_discovery
  irp: LSO Cantata
  rest:
  - productOfferingAvailabilityRequest
  binding: rest
  openapi: openapi/mef-lso-cantata-product-offering-availability-and-pricing-discovery-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: pricingDiscoveryRequest
  category: product_offering_availability_and_pricing_discovery
  irp: LSO Cantata
  rest:
  - pricingDiscoveryRequest
  binding: rest
  openapi: openapi/mef-lso-cantata-product-offering-availability-and-pricing-discovery-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: product_offering_qualification
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: product_offering_qualification
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: product_offering_qualification
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProductOfferingQualification
  category: product_offering_qualification
  irp: LSO Cantata
  rest:
  - listProductOfferingQualification
  binding: rest
  openapi: openapi/mef-lso-cantata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createProductOfferingQualification
  category: product_offering_qualification
  irp: LSO Cantata
  rest:
  - createProductOfferingQualification
  binding: rest
  openapi: openapi/mef-lso-cantata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProductOfferingQualification
  category: product_offering_qualification
  irp: LSO Cantata
  rest:
  - retrieveProductOfferingQualification
  binding: rest
  openapi: openapi/mef-lso-cantata-product-offering-qualification-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listProductOrder
  category: product_order
  irp: LSO Cantata
  rest:
  - listProductOrder
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createProductOrder
  category: product_order
  irp: LSO Cantata
  rest:
  - createProductOrder
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveProductOrder
  category: product_order
  irp: LSO Cantata
  rest:
  - retrieveProductOrder
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: patchProductOrder
  category: product_order
  irp: LSO Cantata
  rest:
  - patchProductOrder
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelProductOrder
  category: product_order
  irp: LSO Cantata
  rest:
  - listCancelProductOrder
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelProductOrder
  category: product_order
  irp: LSO Cantata
  rest:
  - createCancelProductOrder
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelProductOrder
  category: product_order
  irp: LSO Cantata
  rest:
  - retrieveCancelProductOrder
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCharge
  category: product_order
  irp: LSO Cantata
  rest:
  - listCharge
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCharge
  category: product_order
  irp: LSO Cantata
  rest:
  - retrieveCharge
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: patchCharge
  category: product_order
  irp: LSO Cantata
  rest:
  - patchCharge
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: product_order
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: product_order
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: product_order
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyProductOrderItemRequestedDeliveryDate
  category: product_order
  irp: LSO Cantata
  rest:
  - listModifyProductOrderItemRequestedDeliveryDate
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyProductOrderItemRequestedDeliveryDate
  category: product_order
  irp: LSO Cantata
  rest:
  - createModifyProductOrderItemRequestedDeliveryDate
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyProductOrderItemRequestedDeliveryDate
  category: product_order
  irp: LSO Cantata
  rest:
  - retrieveModifyProductOrderItemRequestedDeliveryDate
  binding: rest
  openapi: openapi/mef-lso-cantata-product-order-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listQuote
  category: quote
  irp: LSO Cantata
  rest:
  - listQuote
  binding: rest
  openapi: openapi/mef-lso-cantata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createQuote
  category: quote
  irp: LSO Cantata
  rest:
  - createQuote
  binding: rest
  openapi: openapi/mef-lso-cantata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveQuote
  category: quote
  irp: LSO Cantata
  rest:
  - retrieveQuote
  binding: rest
  openapi: openapi/mef-lso-cantata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: cancelQuote
  category: quote
  irp: LSO Cantata
  rest:
  - cancelQuote
  binding: rest
  openapi: openapi/mef-lso-cantata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: declineQuote
  category: quote
  irp: LSO Cantata
  rest:
  - declineQuote
  binding: rest
  openapi: openapi/mef-lso-cantata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: quote
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: quote
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: quote
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-quote-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTroubleTicket
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - listTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createTroubleTicket
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - createTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTroubleTicket
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - retrieveTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: patchTroubleTicket
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - patchTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: cancelTroubleTicket
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - cancelTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: closeTroubleTicket
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - closeTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: reopenTroubleTicket
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - reopenTroubleTicket
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listIncident
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - listIncident
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveIncident
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - retrieveIncident
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: trouble_ticket_management
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-trouble-ticket-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveWorkOrder
  category: work_order_management
  irp: LSO Cantata
  rest:
  - retrieveWorkOrder
  binding: rest
  openapi: openapi/mef-lso-cantata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listWorkOrder
  category: work_order_management
  irp: LSO Cantata
  rest:
  - listWorkOrder
  binding: rest
  openapi: openapi/mef-lso-cantata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: work_order_management
  irp: LSO Cantata
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-cantata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: work_order_management
  irp: LSO Cantata
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-cantata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: work_order_management
  irp: LSO Cantata
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-cantata-workorder-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listAlarms
  category: alarm_management
  irp: LSO Interlude
  rest:
  - listAlarms
  binding: rest
  openapi: openapi/mef-lso-interlude-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveAlarm
  category: alarm_management
  irp: LSO Interlude
  rest:
  - retrieveAlarm
  binding: rest
  openapi: openapi/mef-lso-interlude-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: alarm_management
  irp: LSO Interlude
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-interlude-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: alarm_management
  irp: LSO Interlude
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-interlude-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: alarm_management
  irp: LSO Interlude
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-interlude-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - listFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - createFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - retrieveFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: resumeFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - resumeFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: suspendFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - suspendFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - listCancelFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - createCancelFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - retrieveCancelFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - listModifyFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - createModifyFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyFaultManagementJob
  category: fault_management
  irp: LSO Interlude
  rest:
  - retrieveModifyFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: faultManagementJobComplexQuery
  category: fault_management
  irp: LSO Interlude
  rest:
  - faultManagementJobComplexQuery
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listFaultManagementReport
  category: fault_management
  irp: LSO Interlude
  rest:
  - listFaultManagementReport
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveFaultManagementReport
  category: fault_management
  irp: LSO Interlude
  rest:
  - retrieveFaultManagementReport
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: faultManagementReportComplexQuery
  category: fault_management
  irp: LSO Interlude
  rest:
  - faultManagementReportComplexQuery
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTrackingRecord
  category: fault_management
  irp: LSO Interlude
  rest:
  - listTrackingRecord
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: fault_management
  irp: LSO Interlude
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: fault_management
  irp: LSO Interlude
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: fault_management
  irp: LSO Interlude
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-interlude-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listPerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - listPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createPerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - createPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrievePerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - retrievePerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: resumePerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - resumePerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: suspendPerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - suspendPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelPerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - listCancelPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelPerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - createCancelPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelPerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - retrieveCancelPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyPerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - listModifyPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyPerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - createModifyPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyPerformanceJob
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - retrieveModifyPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: performanceJobComplexQuery
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - performanceJobComplexQuery
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listPerformanceProfile
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - listPerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createPerformanceProfile
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - createPerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: deletePerformanceProfile
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - deletePerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrievePerformanceProfile
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - retrievePerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: modifyPerformanceProfile
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - modifyPerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listPerformanceReport
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - listPerformanceReport
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createPerformanceReport
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - createPerformanceReport
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrievePerformanceReport
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - retrievePerformanceReport
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: performanceReportComplexQuery
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - performanceReportComplexQuery
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTrackingRecord
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - listTrackingRecord
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: performance_monitoring
  irp: LSO Interlude
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-interlude-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - listTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - createTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - retrieveTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - listCancelTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - createCancelTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - retrieveCancelTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listSuspendTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - listSuspendTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createSuspendTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - createSuspendTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveSuspendTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - retrieveSuspendTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listResumeTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - listResumeTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createResumeTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - createResumeTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveResumeTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - retrieveResumeTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - listModifyTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - createModifyTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyTestJob
  category: service_function_test
  irp: LSO Interlude
  rest:
  - retrieveModifyTestJob
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTestProfile
  category: service_function_test
  irp: LSO Interlude
  rest:
  - listTestProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createTestProfile
  category: service_function_test
  irp: LSO Interlude
  rest:
  - createTestProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTestProfile
  category: service_function_test
  irp: LSO Interlude
  rest:
  - retrieveTestProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: deleteTestProfile
  category: service_function_test
  irp: LSO Interlude
  rest:
  - deleteTestProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: modifyTestProfile
  category: service_function_test
  irp: LSO Interlude
  rest:
  - modifyTestProfile
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTestResult
  category: service_function_test
  irp: LSO Interlude
  rest:
  - listTestResult
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTestResult
  category: service_function_test
  irp: LSO Interlude
  rest:
  - retrieveTestResult
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: service_function_test
  irp: LSO Interlude
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: service_function_test
  irp: LSO Interlude
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: service_function_test
  irp: LSO Interlude
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-interlude-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: serviceFind
  category: service_inventory_management
  irp: LSO Interlude
  rest:
  - serviceFind
  binding: rest
  openapi: openapi/mef-lso-interlude-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: serviceGet
  category: service_inventory_management
  irp: LSO Interlude
  rest:
  - serviceGet
  binding: rest
  openapi: openapi/mef-lso-interlude-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: service_inventory_management
  irp: LSO Interlude
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-interlude-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: service_inventory_management
  irp: LSO Interlude
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-interlude-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: service_inventory_management
  irp: LSO Interlude
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-interlude-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listServiceOrder
  category: service_ordering_management
  irp: LSO Interlude
  rest:
  - listServiceOrder
  binding: rest
  openapi: openapi/mef-lso-interlude-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createServiceOrder
  category: service_ordering_management
  irp: LSO Interlude
  rest:
  - createServiceOrder
  binding: rest
  openapi: openapi/mef-lso-interlude-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveServiceOrder
  category: service_ordering_management
  irp: LSO Interlude
  rest:
  - retrieveServiceOrder
  binding: rest
  openapi: openapi/mef-lso-interlude-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: service_ordering_management
  irp: LSO Interlude
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-interlude-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveEventSubscription
  category: service_ordering_management
  irp: LSO Interlude
  rest:
  - retrieveEventSubscription
  binding: rest
  openapi: openapi/mef-lso-interlude-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: service_ordering_management
  irp: LSO Interlude
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-interlude-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: topicList
  category: streaming_management
  irp: LSO Interlude
  rest:
  - topicList
  binding: rest
  openapi: openapi/mef-lso-interlude-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: topicById
  category: streaming_management
  irp: LSO Interlude
  rest:
  - topicById
  binding: rest
  openapi: openapi/mef-lso-interlude-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: subscriptionList
  category: streaming_management
  irp: LSO Interlude
  rest:
  - subscriptionList
  binding: rest
  openapi: openapi/mef-lso-interlude-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: subscribeTopic
  category: streaming_management
  irp: LSO Interlude
  rest:
  - subscribeTopic
  binding: rest
  openapi: openapi/mef-lso-interlude-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unsubscribeTopic
  category: streaming_management
  irp: LSO Interlude
  rest:
  - unsubscribeTopic
  binding: rest
  openapi: openapi/mef-lso-interlude-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: subscriptionById
  category: streaming_management
  irp: LSO Interlude
  rest:
  - subscriptionById
  binding: rest
  openapi: openapi/mef-lso-interlude-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listAlarms
  category: alarm_management
  irp: LSO Legato
  rest:
  - listAlarms
  binding: rest
  openapi: openapi/mef-lso-legato-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveAlarm
  category: alarm_management
  irp: LSO Legato
  rest:
  - retrieveAlarm
  binding: rest
  openapi: openapi/mef-lso-legato-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: alarm_management
  irp: LSO Legato
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-legato-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: alarm_management
  irp: LSO Legato
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-legato-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: alarm_management
  irp: LSO Legato
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-legato-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - listFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - createFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - retrieveFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: resumeFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - resumeFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: suspendFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - suspendFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - listCancelFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - createCancelFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - retrieveCancelFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - listModifyFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - createModifyFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyFaultManagementJob
  category: fault_management
  irp: LSO Legato
  rest:
  - retrieveModifyFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: faultManagementJobComplexQuery
  category: fault_management
  irp: LSO Legato
  rest:
  - faultManagementJobComplexQuery
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listFaultManagementReport
  category: fault_management
  irp: LSO Legato
  rest:
  - listFaultManagementReport
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveFaultManagementReport
  category: fault_management
  irp: LSO Legato
  rest:
  - retrieveFaultManagementReport
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: faultManagementReportComplexQuery
  category: fault_management
  irp: LSO Legato
  rest:
  - faultManagementReportComplexQuery
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTrackingRecord
  category: fault_management
  irp: LSO Legato
  rest:
  - listTrackingRecord
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: fault_management
  irp: LSO Legato
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: fault_management
  irp: LSO Legato
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: fault_management
  irp: LSO Legato
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-legato-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listPerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - listPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createPerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - createPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrievePerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - retrievePerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: resumePerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - resumePerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: suspendPerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - suspendPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelPerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - listCancelPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelPerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - createCancelPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelPerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - retrieveCancelPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyPerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - listModifyPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyPerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - createModifyPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyPerformanceJob
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - retrieveModifyPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: performanceJobComplexQuery
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - performanceJobComplexQuery
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listPerformanceProfile
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - listPerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createPerformanceProfile
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - createPerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: deletePerformanceProfile
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - deletePerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrievePerformanceProfile
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - retrievePerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: modifyPerformanceProfile
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - modifyPerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listPerformanceReport
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - listPerformanceReport
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createPerformanceReport
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - createPerformanceReport
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrievePerformanceReport
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - retrievePerformanceReport
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: performanceReportComplexQuery
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - performanceReportComplexQuery
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTrackingRecord
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - listTrackingRecord
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: performance_monitoring
  irp: LSO Legato
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-legato-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - listTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - createTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - retrieveTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - listCancelTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - createCancelTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - retrieveCancelTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listSuspendTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - listSuspendTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createSuspendTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - createSuspendTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveSuspendTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - retrieveSuspendTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listResumeTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - listResumeTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createResumeTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - createResumeTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveResumeTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - retrieveResumeTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - listModifyTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - createModifyTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyTestJob
  category: service_function_test
  irp: LSO Legato
  rest:
  - retrieveModifyTestJob
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTestProfile
  category: service_function_test
  irp: LSO Legato
  rest:
  - listTestProfile
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createTestProfile
  category: service_function_test
  irp: LSO Legato
  rest:
  - createTestProfile
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTestProfile
  category: service_function_test
  irp: LSO Legato
  rest:
  - retrieveTestProfile
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: deleteTestProfile
  category: service_function_test
  irp: LSO Legato
  rest:
  - deleteTestProfile
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: modifyTestProfile
  category: service_function_test
  irp: LSO Legato
  rest:
  - modifyTestProfile
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTestResult
  category: service_function_test
  irp: LSO Legato
  rest:
  - listTestResult
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTestResult
  category: service_function_test
  irp: LSO Legato
  rest:
  - retrieveTestResult
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: service_function_test
  irp: LSO Legato
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: service_function_test
  irp: LSO Legato
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: service_function_test
  irp: LSO Legato
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-legato-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: serviceFind
  category: service_inventory_management
  irp: LSO Legato
  rest:
  - serviceFind
  binding: rest
  openapi: openapi/mef-lso-legato-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: serviceGet
  category: service_inventory_management
  irp: LSO Legato
  rest:
  - serviceGet
  binding: rest
  openapi: openapi/mef-lso-legato-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: service_inventory_management
  irp: LSO Legato
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-legato-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: service_inventory_management
  irp: LSO Legato
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-legato-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: service_inventory_management
  irp: LSO Legato
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-legato-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listServiceOrder
  category: service_ordering_management
  irp: LSO Legato
  rest:
  - listServiceOrder
  binding: rest
  openapi: openapi/mef-lso-legato-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createServiceOrder
  category: service_ordering_management
  irp: LSO Legato
  rest:
  - createServiceOrder
  binding: rest
  openapi: openapi/mef-lso-legato-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveServiceOrder
  category: service_ordering_management
  irp: LSO Legato
  rest:
  - retrieveServiceOrder
  binding: rest
  openapi: openapi/mef-lso-legato-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: service_ordering_management
  irp: LSO Legato
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-legato-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveEventSubscription
  category: service_ordering_management
  irp: LSO Legato
  rest:
  - retrieveEventSubscription
  binding: rest
  openapi: openapi/mef-lso-legato-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: service_ordering_management
  irp: LSO Legato
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-legato-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: topicList
  category: streaming_management
  irp: LSO Legato
  rest:
  - topicList
  binding: rest
  openapi: openapi/mef-lso-legato-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: topicById
  category: streaming_management
  irp: LSO Legato
  rest:
  - topicById
  binding: rest
  openapi: openapi/mef-lso-legato-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: subscriptionList
  category: streaming_management
  irp: LSO Legato
  rest:
  - subscriptionList
  binding: rest
  openapi: openapi/mef-lso-legato-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: subscribeTopic
  category: streaming_management
  irp: LSO Legato
  rest:
  - subscribeTopic
  binding: rest
  openapi: openapi/mef-lso-legato-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unsubscribeTopic
  category: streaming_management
  irp: LSO Legato
  rest:
  - unsubscribeTopic
  binding: rest
  openapi: openapi/mef-lso-legato-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: subscriptionById
  category: streaming_management
  irp: LSO Legato
  rest:
  - subscriptionById
  binding: rest
  openapi: openapi/mef-lso-legato-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listAlarms
  category: alarm_management
  irp: LSO Allegro
  rest:
  - listAlarms
  binding: rest
  openapi: openapi/mef-lso-allegro-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveAlarm
  category: alarm_management
  irp: LSO Allegro
  rest:
  - retrieveAlarm
  binding: rest
  openapi: openapi/mef-lso-allegro-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: alarm_management
  irp: LSO Allegro
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-allegro-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: alarm_management
  irp: LSO Allegro
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-allegro-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: alarm_management
  irp: LSO Allegro
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-allegro-alarm-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - listFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - createFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - retrieveFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: resumeFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - resumeFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: suspendFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - suspendFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - listCancelFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - createCancelFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - retrieveCancelFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - listModifyFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - createModifyFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyFaultManagementJob
  category: fault_management
  irp: LSO Allegro
  rest:
  - retrieveModifyFaultManagementJob
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: faultManagementJobComplexQuery
  category: fault_management
  irp: LSO Allegro
  rest:
  - faultManagementJobComplexQuery
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listFaultManagementReport
  category: fault_management
  irp: LSO Allegro
  rest:
  - listFaultManagementReport
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveFaultManagementReport
  category: fault_management
  irp: LSO Allegro
  rest:
  - retrieveFaultManagementReport
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: faultManagementReportComplexQuery
  category: fault_management
  irp: LSO Allegro
  rest:
  - faultManagementReportComplexQuery
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTrackingRecord
  category: fault_management
  irp: LSO Allegro
  rest:
  - listTrackingRecord
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: fault_management
  irp: LSO Allegro
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: fault_management
  irp: LSO Allegro
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: fault_management
  irp: LSO Allegro
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-allegro-fault-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listPerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - listPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createPerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - createPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrievePerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - retrievePerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: resumePerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - resumePerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: suspendPerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - suspendPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelPerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - listCancelPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelPerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - createCancelPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelPerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - retrieveCancelPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyPerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - listModifyPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyPerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - createModifyPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyPerformanceJob
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - retrieveModifyPerformanceJob
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: performanceJobComplexQuery
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - performanceJobComplexQuery
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listPerformanceProfile
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - listPerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createPerformanceProfile
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - createPerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: deletePerformanceProfile
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - deletePerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrievePerformanceProfile
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - retrievePerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: modifyPerformanceProfile
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - modifyPerformanceProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listPerformanceReport
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - listPerformanceReport
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createPerformanceReport
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - createPerformanceReport
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrievePerformanceReport
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - retrievePerformanceReport
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: performanceReportComplexQuery
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - performanceReportComplexQuery
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTrackingRecord
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - listTrackingRecord
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: performance_monitoring
  irp: LSO Allegro
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-allegro-performance-monitoring-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - listTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - createTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - retrieveTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listCancelTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - listCancelTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createCancelTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - createCancelTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveCancelTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - retrieveCancelTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listSuspendTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - listSuspendTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createSuspendTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - createSuspendTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveSuspendTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - retrieveSuspendTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listResumeTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - listResumeTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createResumeTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - createResumeTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveResumeTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - retrieveResumeTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listModifyTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - listModifyTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createModifyTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - createModifyTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveModifyTestJob
  category: service_function_test
  irp: LSO Allegro
  rest:
  - retrieveModifyTestJob
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTestProfile
  category: service_function_test
  irp: LSO Allegro
  rest:
  - listTestProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createTestProfile
  category: service_function_test
  irp: LSO Allegro
  rest:
  - createTestProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTestProfile
  category: service_function_test
  irp: LSO Allegro
  rest:
  - retrieveTestProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: deleteTestProfile
  category: service_function_test
  irp: LSO Allegro
  rest:
  - deleteTestProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: modifyTestProfile
  category: service_function_test
  irp: LSO Allegro
  rest:
  - modifyTestProfile
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listTestResult
  category: service_function_test
  irp: LSO Allegro
  rest:
  - listTestResult
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveTestResult
  category: service_function_test
  irp: LSO Allegro
  rest:
  - retrieveTestResult
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: service_function_test
  irp: LSO Allegro
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: service_function_test
  irp: LSO Allegro
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: service_function_test
  irp: LSO Allegro
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-allegro-service-function-test-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: serviceFind
  category: service_inventory_management
  irp: LSO Allegro
  rest:
  - serviceFind
  binding: rest
  openapi: openapi/mef-lso-allegro-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: serviceGet
  category: service_inventory_management
  irp: LSO Allegro
  rest:
  - serviceGet
  binding: rest
  openapi: openapi/mef-lso-allegro-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: service_inventory_management
  irp: LSO Allegro
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-allegro-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveHub
  category: service_inventory_management
  irp: LSO Allegro
  rest:
  - retrieveHub
  binding: rest
  openapi: openapi/mef-lso-allegro-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: service_inventory_management
  irp: LSO Allegro
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-allegro-service-inventory-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: listServiceOrder
  category: service_ordering_management
  irp: LSO Allegro
  rest:
  - listServiceOrder
  binding: rest
  openapi: openapi/mef-lso-allegro-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: createServiceOrder
  category: service_ordering_management
  irp: LSO Allegro
  rest:
  - createServiceOrder
  binding: rest
  openapi: openapi/mef-lso-allegro-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveServiceOrder
  category: service_ordering_management
  irp: LSO Allegro
  rest:
  - retrieveServiceOrder
  binding: rest
  openapi: openapi/mef-lso-allegro-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: registerListener
  category: service_ordering_management
  irp: LSO Allegro
  rest:
  - registerListener
  binding: rest
  openapi: openapi/mef-lso-allegro-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: retrieveEventSubscription
  category: service_ordering_management
  irp: LSO Allegro
  rest:
  - retrieveEventSubscription
  binding: rest
  openapi: openapi/mef-lso-allegro-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unregisterListener
  category: service_ordering_management
  irp: LSO Allegro
  rest:
  - unregisterListener
  binding: rest
  openapi: openapi/mef-lso-allegro-service-ordering-management-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: topicList
  category: streaming_management
  irp: LSO Allegro
  rest:
  - topicList
  binding: rest
  openapi: openapi/mef-lso-allegro-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: topicById
  category: streaming_management
  irp: LSO Allegro
  rest:
  - topicById
  binding: rest
  openapi: openapi/mef-lso-allegro-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: subscriptionList
  category: streaming_management
  irp: LSO Allegro
  rest:
  - subscriptionList
  binding: rest
  openapi: openapi/mef-lso-allegro-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: subscribeTopic
  category: streaming_management
  irp: LSO Allegro
  rest:
  - subscribeTopic
  binding: rest
  openapi: openapi/mef-lso-allegro-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: unsubscribeTopic
  category: streaming_management
  irp: LSO Allegro
  rest:
  - unsubscribeTopic
  binding: rest
  openapi: openapi/mef-lso-allegro-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
- tool: subscriptionById
  category: streaming_management
  irp: LSO Allegro
  rest:
  - subscriptionById
  binding: rest
  openapi: openapi/mef-lso-allegro-streaming-management-all-in-one-openapi.yml
  confidence: high
  note: FastMCP.from_openapi() names each tool after the operationId of the backing operation; the tool
    inherits that operation’s parameters and requestBody as its inputSchema.
mcp_only: []
rest_only:
- reason: no MCP server shipped
  documents:
  - openapi: openapi/mef-lso-allegro-streaming-management-openapi.yml
    operations: 6
  - openapi: openapi/mef-lso-interlude-streaming-management-openapi.yml
    operations: 6
  - openapi: openapi/mef-lso-legato-service-catalog-api-openapi.yml
    operations: 5
  - openapi: openapi/mef-lso-legato-streaming-management-openapi.yml
    operations: 6
- reason: notification/listener
  documents:
  - openapi: openapi/mef-lso-allegro-alarm-notification-openapi.yml
    operations: 4
  - openapi: openapi/mef-lso-allegro-fault-notification-openapi.yml
    operations: 9
  - openapi: openapi/mef-lso-allegro-performance-notification-openapi.yml
    operations: 12
  - openapi: openapi/mef-lso-allegro-service-function-test-notification-openapi.yml
    operations: 12
  - openapi: openapi/mef-lso-allegro-service-inventory-notification-openapi.yml
    operations: 4
  - openapi: openapi/mef-lso-allegro-service-ordering-notification-openapi.yml
    operations: 4
  - openapi: openapi/mef-lso-cantata-appointment-notification-openapi.yml
    operations: 2
  - openapi: openapi/mef-lso-cantata-billing-notification-openapi.yml
    operations: 2
  - openapi: openapi/mef-lso-cantata-circuit-impairment-and-maintenance-notification-openapi.yml
    operations: 3
  - openapi: openapi/mef-lso-cantata-geographic-address-notification-openapi.yml
    operations: 1
  - openapi: openapi/mef-lso-cantata-product-catalog-notification-openapi.yml
    operations: 8
  - openapi: openapi/mef-lso-cantata-product-offering-qualification-notification-openapi.yml
    operations: 2
  - openapi: openapi/mef-lso-cantata-product-order-notification-openapi.yml
    operations: 9
  - openapi: openapi/mef-lso-cantata-quote-notification-openapi.yml
    operations: 2
  - openapi: openapi/mef-lso-cantata-trouble-ticket-notification-openapi.yml
    operations: 7
  - openapi: openapi/mef-lso-cantata-workorder-notification-openapi.yml
    operations: 3
  - openapi: openapi/mef-lso-interlude-alarm-notification-openapi.yml
    operations: 4
  - openapi: openapi/mef-lso-interlude-fault-notification-openapi.yml
    operations: 9
  - openapi: openapi/mef-lso-interlude-performance-notification-openapi.yml
    operations: 12
  - openapi: openapi/mef-lso-interlude-service-function-test-notification-openapi.yml
    operations: 12
  - openapi: openapi/mef-lso-interlude-service-inventory-notification-openapi.yml
    operations: 4
  - openapi: openapi/mef-lso-interlude-service-ordering-notification-openapi.yml
    operations: 4
  - openapi: openapi/mef-lso-legato-alarm-notification-openapi.yml
    operations: 4
  - openapi: openapi/mef-lso-legato-fault-notification-openapi.yml
    operations: 9
  - openapi: openapi/mef-lso-legato-performance-notification-openapi.yml
    operations: 12
  - openapi: openapi/mef-lso-legato-service-catalog-notification-api-openapi.yml
    operations: 3
  - openapi: openapi/mef-lso-legato-service-function-test-notification-openapi.yml
    operations: 12
  - openapi: openapi/mef-lso-legato-service-inventory-notification-openapi.yml
    operations: 4
  - openapi: openapi/mef-lso-legato-service-ordering-notification-openapi.yml
    operations: 4
  - openapi: openapi/mef-lso-sonata-appointment-notification-openapi.yml
    operations: 2
  - openapi: openapi/mef-lso-sonata-billing-notification-openapi.yml
    operations: 2
  - openapi: openapi/mef-lso-sonata-circuit-impairment-and-maintenance-notification-openapi.yml
    operations: 3
  - openapi: openapi/mef-lso-sonata-geographic-address-notification-openapi.yml
    operations: 1
  - openapi: openapi/mef-lso-sonata-product-catalog-notification-openapi.yml
    operations: 8
  - openapi: openapi/mef-lso-sonata-product-offering-qualification-notification-openapi.yml
    operations: 2
  - openapi: openapi/mef-lso-sonata-product-order-notification-openapi.yml
    operations: 9
  - openapi: openapi/mef-lso-sonata-quote-notification-openapi.yml
    operations: 2
  - openapi: openapi/mef-lso-sonata-trouble-ticket-notification-openapi.yml
    operations: 7
  - openapi: openapi/mef-lso-sonata-workorder-notification-openapi.yml
    operations: 3
